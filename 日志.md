# 项目变更日志（日志.md）

> 本文件记录项目每一轮对话与代码变更的完整历史。规则：每次对话/更改结束后，在文件末尾追加一条记录（不清空、不覆盖），包含：时间、主题/背景、文件清单（新增/修改/删除）、关键实现要点、验证结果、落盘状态。

---

## 历史记录（由 git 历史回溯整理）

### 2026-09-20 · first commit（zhpoj）
- 提交：`4247999`
- 背景：项目初始化，建立基础工程结构。
- 文件清单（新增）：
  - `.gitignore`
  - `package-lock.json`、`package.json`
  - `src/app.ts`、`src/index.ts`
  - `tests/app.test.ts`
  - `tsconfig.json`
- 关键实现要点：建立最小可运行的服务端入口（src/index.ts、src/app.ts），配置 TypeScript（tsconfig.json）与基础测试（tests/app.test.ts）。
- 验证结果：初始提交，无额外验证记录。
- 落盘状态：已提交（committed）。

### 2026-09-20 · 功能：使用 React 和 Vite 初始化网页项目（zhpoj）
- 提交：`18044d3`
- 背景：引入前端 Web 工程，与后端 Agent 能力整合。
- 文件清单（新增/修改）：
  - 新增 `web/` 前端工程：`web/package.json`、`web/index.html`、`web/vite.config.ts`、`web/tsconfig*.json`、`web/eslint.config.js`
  - 新增 `web/src/`：`main.tsx`、`App.tsx`、`App.css`、`index.css`，资源 `hero.png`、`react.svg`、`vite.svg`
  - 新增 `web/public/`：`favicon.svg`、`icons.svg`；`web/README.md`；`web/.gitignore`
  - 新增 `src/agent/run.ts`、`src/agent/engine.ts`、`tests/agent.test.ts`
  - 修改 `src/app.ts`、`src/index.ts`、`package.json`、`tsconfig.json`、`package-lock.json`
- 关键实现要点：
  - Web 端用 Vite + React + TypeScript 搭建，含亮色/暗色主题全局样式（App.css、index.css）。
  - 后端新增 Agent 运行时（run.ts）与引擎（engine.ts），提供 Agent 测试（agent.test.ts）。
  - 共 27 个文件变更，+7267 / -7。
- 验证结果：随提交附带测试文件，无额外验证记录。
- 落盘状态：已提交（committed）。

---

## 当前工作区未提交变更（截至 2026-09-22 会话开始时）

> 以下为会话开始时工作区中尚未 commit 的改动，作为历史快照记录。

- 已修改（未提交）：`package.json`、`src/agent/engine.ts`、`src/index.ts`
- 新增（未跟踪）：`.dockerignore`、`.env.example`、`Dockerfile`、`docker-compose.yml`、`src/agent/workspace.ts`、`tests/workspace.test.ts`、`tsconfig.build.json`、`workspace/`
- 落盘状态：未提交（uncommitted / working tree）。

---

## 2026-09-22 · 建立日志机制（用户请求）
- 主题/背景：用户要求创建并维护 `日志.md`，后续每轮对话/更改都追加记录，并把已有 git 历史补录到日志中。
- 文件清单（新增/修改）：
  - 新增/重写 `日志.md`（补录 git 历史 2 个提交 + 当前未提交工作区快照）。
- 关键实现要点：
  - 通过 `git log --stat` 回溯导出历史提交明细，整理为结构化日志记录。
  - 建立"每次会话末尾追加"的约定（见文件头），并由记忆（memory）确保后续自动执行。
- 验证结果：日志文件已写入，历史记录完整。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-22 15:58 · 前端控制台接入 runs/prompt/events 三接口（用户请求）
- 主题/背景：用户要求在已有 Vite + React 前端工程上搭一个控制台，把后端 `runs`（创建/列表）、`prompt`（投递）、`events`（增量拉取）三个接口接起来。
- 文件清单（新增/修改）：
  - 新增 `web/src/lib/api.ts`（三个接口的 TS 客户端 + `ApiError`）
  - 新增 `web/src/components/CreateRunForm.tsx`、`RunList.tsx`、`RunDetail.tsx`、`PromptComposer.tsx`、`EventFeed.tsx`
  - 重写 `web/src/App.tsx`（控制台主界面与轮询逻辑）、`web/src/App.css`（控制台样式）
  - 修改 `web/vite.config.ts`（`/api` 代理到 `http://localhost:3000`，可用 `API_TARGET` 覆盖）、`web/index.html`（标题）、`web/src/index.css`（`#root` 宽度放宽到 1400px）、`web/README.md`（接口对照表与启动方式）
- 关键实现要点：
  - api.ts 用统一 `request<T>()` 处理 JSON 解析与非 2xx（把 `{error, message, issues}` 提升为 `ApiError`），网络失败映射为 `code: 'network_error'`；`VITE_API_BASE` 默认 `/api`。
  - Events 用轮询增量：`fetchEvents(runId, cursorRef.current)`，`since = 最后一条 seq + 1`；App 顶层 `tick()` 同时刷新 runs 列表与选中 run 快照，挂载后每 1.5s 轮询（顶部「自动轮询」开关可关），渲染窗口保留最近 500 条。
  - 为规避 react-hooks `set-state-in-effect` 规则，effect 内用 `setTimeout(..., 0)` 异步触发首次 `tick()`，cleanup 同时清理 timer 与 interval。
  - UI：左侧 Runs 列表 + 新建 run 表单（cwd/model/thinkingLevel/tools/systemPrompt/persistSession，空值走后端默认），右侧 run 快照卡片 + prompt 输入框（streamingBehavior 可选 auto/steer/followUp，Ctrl+Enter 发送）+ 事件流（类型/内容过滤、自动滚动、payload JSON 高亮块）。
- 验证结果：`npm run build`（tsc -b && vite build）通过；`npm run lint` 无告警（修复了一处 effect 内同步 setState 报错）；冒烟——临时 mock 后端（已删除）+ vite dev 实测 `GET /api/agent/health`、`GET /api/agent/runs`、`GET /api/agent/runs/:id/events?since=0` 经 5173 代理转发均返回正确 JSON，首页 200。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-22 16:10 · 澄清前端架构文档 + 确定范围（用户请求）
- 主题/背景：用户贴出一段把 web/ 描述为 React Router v7 + Zustand 5 + SSE 流式管线的“产品工作台”架构设想文档，表示看不懂并问该怎么做。澄清：当前 `web/` 实为我刚搭的轻量单页控制台（仅 useState/useRef，轮询 events，无 Router/Zustand/SSE），文档描述的是比现状多数倍的未来产品蓝图。
- 文件清单（修改）：无代码改动。仅追加 `日志.md` 本轮记录。
- 关键实现要点：向用户直白拆解三根支柱（页面路由、全局状态、流式事件管线）及其"高频流式下渲染性能"约束；指出当前代码与文档的差异；给出 A(保持轻量)/B(升级 SSE)/C(落地完整架构) 三选项。用户选择 A：能跑、能调试后端三接口即可，前端到此为止，不引入 Router/Zustand/SSE。
- 验证结果：纯讨论，无构建/冒烟。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-22 16:35 · 前端接入 SSE 实时事件流（用户请求）
- 主题/背景：用户决定保留轻量控制台（A），但额外要求"加个流式输出"。后端已有 `GET /api/agent/runs/:id/stream`（SSE）接口，前端据此用实时流替换原来的 1.5s 轮询（即此前提出的 B 方案）。
- 文件清单（修改）：
  - `web/src/lib/api.ts`：新增 `fetchRun(id)`（`GET /api/agent/runs/:id`，状态卡片用）+ `subscribeRun(id, since, handlers)`（fetch + ReadableStream 自解析 SSE，返回取消函数；`onRun`/`onEvent`/`onError` 回调，按 `event:` 字段分流 `run` 快照与各类事件帧）。
  - `web/src/App.tsx`：用 `useEffect` 订阅选中 run 的 SSE 流（`since=0` 全量回放 + 实时推送，按 `seq` 去重），事件实时追加到事件流；保留 2s 低频 `META_REFRESH_MS` 刷新只用于更新 run 状态卡片与左侧列表；顶部开关改为"自动刷新状态"，"刷新"按钮改为 `refreshKey` 触发重订阅+重拉元数据；移除对 `fetchEvents` 轮询的依赖。
  - `web/index.html` 副标题微调为 `runs / prompt / events · SSE 实时事件`。
- 关键实现要点：
  - SSE 用 `fetch` 而非 `EventSource`，因为服务端用命名事件（`event: run` / `event: <type>`），自解析可覆盖任意事件类型且无需同源/cookie 限制；`AbortController` 在切 run 或卸载时关闭流。
  - 去重：维护 `lastSeqRef`，`event.seq <= lastSeqRef` 丢弃，避免重连/回放重复；渲染窗口保留最近 500 条。
  - 流式与状态分离：事件走 SSE（高频、实时），状态卡片/列表走 2s 元数据轮询（低频）。
- 验证结果：`npm run lint` 与 `npm run build` 均通过；端到端冒烟——临时脚本在本进程内拉起 mock 后端(3000)+vite(5173)，经 5173 代理请求 `/api/agent/runs/r1/stream?since=0`，返回 `200 / text/event-stream`，并正确解析出 `run` 快照帧与 `agent_start`/`message_update` 事件帧（探针脚本已删除）。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-22 16:50 · 新增一键启动脚本（用户请求）
- 主题/背景：用户希望"一个命令跑通看效果"，不想开两个终端分别起后端和前端。
- 文件清单（新增/修改）：
  - 新增 `run-dev.mjs`（根目录）：用 `child_process.spawn` 同时拉起根目录后端（`npm run dev` → `tsx watch src/index.ts`，:3000）与前端（`npm --prefix web run dev` → vite，:5173）；监听 `SIGINT`/`SIGTERM` 一并杀掉两个子进程；`stdio: 'inherit` 透传日志，`shell: true` 跨平台解析 `npm`。
  - 修改 `package.json`：新增脚本 `"dev:full": "node run-dev.mjs"`（保留原 `"dev"` 仅起后端）。
- 关键实现要点：零新增依赖（复用已有的 tsx）；启动后打印 "后端 :3000 + 前端 :5173，Ctrl+C 退出" 提示；Ctrl+C 时父进程向两个子进程发 `SIGTERM` 并 300ms 后退出。
- 验证结果：`node --check run-dev.mjs` 语法通过（无阻塞运行验证；后端/前端各自启动方式此前均已验证可用）。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-22 17:05 · 讨论：IM 渠道接入层架构可行性（用户请求）
- 主题/背景：用户贴出一段把 miniclaw 描述为"七个异构 IM 平台 → 统一渠道接入层（IMChannel 契约 + 各平台适配器）"的设计文档摘要，问"我做这个行不行"。先检索确认仓库现状。
- 文件清单（修改）：无代码改动。仅追加 `日志.md` 本轮记录。
- 关键实现要点：检索 `src/` 确认**无任何 IM/渠道/适配器代码**（仅 `src/index.ts` 的 `signal` 信号处理为误匹配），当前仓库只是 MiniAgent 引擎 + React 控制台；该文档描述的是从零开始的大型新子系统。向用户拆解三块：①统一契约 IMChannel；②各平台协议/认证差异（Telegram Bot/Webhook、Discord Gateway WS+OAuth、Slack Events API 等）；③各适配器独有能力（流式卡片、持久化收件箱、重连策略）；并提示文档边界——多账号绑定规则与投递状态机属后续两页。可行性判断：架构成熟可行，但体量大，建议增量路径"契约→单平台示例→横向扩展"，不要一次铺开七平台。给出 A(讲透)/B(契约骨架)/C(首个适配器)/D(规划全量) 选项待用户选择。
- 验证结果：纯讨论，无构建/冒烟。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-23 09:10 · 打通第 1 步「飞书消息 → agent run」（用户请求）
- 主题/背景：上一轮讨论确定了"契约 → 单平台示例 → 横向扩展"的增量路径，本轮做第 1 步：让飞书里发的一条消息真的变成一个 agent run，并把结果发回群里。此前 `src/im/` 只有 IMChannel 契约 + 飞书适配器骨架（token/签名/加解密/webhook 解析），`dispatch` 出来后没有任何消费方。
- 文件清单（新增/修改）：
  - 新增 `src/im/bridge.ts`（核心）：`IMBridge` 会话桥接
  - 新增 `tests/im-bridge.test.ts`（14 条用例）
  - 新增 `test_feishu.mjs`（端到端冒烟）
  - 修改 `src/im/index.ts`：mount 时接 engine，注册 bridge、新增 `/api/im/conversations`、导出 `getMountedIM`
  - 修改 `src/app.ts`：`mountIMChannels(app, { engine })`
  - 修改 `src/index.ts`：启动时打印挂载的 IM 通道与 webhook 路径
  - 修改 `.env.example`：新增 IM 配置段
- 关键实现要点：
  - **绑定粒度**：`channelId:conversationId` → 一个 `AgentRun`，所以每个群/单聊各自持有上下文；数量上限由 `IM_MAX_CONVERSATIONS` 控制，超了按 LRU 关掉闲置会话。
  - **webhook 必须秒回**：飞书约 1s 就判定投递失败，所以 `bridge.handle()` 只做"接住 + 入队"就返回，回合在后台跑完再回消息；`attach(channel)` 内部用 `void this.handle()` 订阅，天然不阻塞。
  - **回合串行 + 排队**：同一会话有回合在跑时，新消息进有界队列（`IM_MAX_QUEUE`），满了回"队列也满了"；回合结束后 `drain()` 自动接着跑下一条。
  - **答案抽取**：`collectAssistantText()` 订阅 run 事件，`message_end` 取 assistant 完整文本，`message_update` 取 `text_delta` 兜底（超时/中断也能拿到半截）。
  - **健壮性**：`turnTimeoutMs` 超时保护（默认 10min）、发送失败只记日志不抛、`trySend` 兜住网络错误、忽略非 assistant 消息、未注册通道的消息直接丢弃并告警。
  - **命令**：`/help` `/new`(`/reset`) `/status` `/abort`；`/status` 打出 run id、模型、工具、cwd、事件数、队列深度。
  - **可选流式**：`IM_STREAMING=1` 时先发一张「正在处理…」卡片，再按 `IM_STREAM_INTERVAL_MS` 节流 `PATCH` 回填；默认关闭，走单条文本。
  - **可测试性**：依赖收敛为 `IMBridgeEngine = Pick<AgentEngine,'createRun'>` 与 `IMRun = Pick<AgentRun, ...7 个成员>`，测试用 fake run/channel 完全不碰真实模型。
- 验证结果：
  - `npx tsc --noEmit` 通过；`npm run build` 通过（产物 `dist/im/bridge.js`）。
  - `npx vitest run` 全绿：**44 项通过**（新增 im-bridge 14 项：首条消息→回复、delta 兜底、会话复用与隔离、忙时排队、队列上限、/new 重开会建新 run、/status、/abort、失败回传、超时、超长截断、流式回填、未注册通道不崩、dispose 关 run）；原有 app/im/agent/workspace 用例无回归。
  - 端到端 `node test_feishu.mjs`（真模型 deepseek/deepseek-v4-pro）：假飞书开放平台收到 **send/text "OK"**，耗时 ~11s；`--stream` 分支收到 **update/interactive 卡片回填 "OK"**（`om_1`），耗时 ~8s。
- 落盘状态：已写入工作区（未提交）。

### 2026-09-23 16:29 · 飞书真实长连接接入与端到端验证（用户请求）
- 主题/背景：用户要求在飞书开放平台配置真实企业自建应用并完成可实际使用的消息回调测试。最终采用飞书官方 WebSocket 长连接，不使用公网回调地址或内网穿透。
- 文件清单（新增/修改）：
  - 新增 `飞书机器人真实接入操作记录.md`，记录后台配置、本地启动、测试过程、故障原因、日常使用和后续部署建议。
  - 修改 `src/im/feishu.ts`：接入官方长连接、增加生命周期控制，并兼容 `post` 富文本消息。
  - 修改 `src/im/index.ts`、`src/index.ts`：服务启动/关闭时管理 IM 长连接。
  - 修改 `tests/im.test.ts`：增加长连接和富文本消息回归测试。
  - 修改 `.env.example`、`package.json`、`package-lock.json`：补充长连接配置和官方 SDK 依赖。
- 关键实现要点：飞书后台在“事件配置”选择长连接，订阅 `im.message.receive_v1`，发布应用版本；本地使用 App ID/Secret 建立长连接。真实消息最初以 `message_type: post` 到达而被旧代码过滤，加入富文本解析后，消息成功进入 Agent Run，并把 DeepSeek 回复发送回飞书。
- 验证结果：真实飞书消息接收成功，`deepseek/deepseek-v4-pro` 正常生成回复，用户确认飞书客户端收到回复；`npm test` 49 项通过，`npm run typecheck` 与 `npm run build` 通过。
- 安全提醒：调试期间密钥曾出现在聊天中，操作记录不保存真实密钥，并要求尽快轮换 DeepSeek API Key 与飞书 App Secret。
- 落盘状态：已写入工作区（未提交）。
