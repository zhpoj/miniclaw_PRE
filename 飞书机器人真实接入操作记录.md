# 飞书机器人真实接入操作记录

> 日期：2026-09-23  
> 项目：`miniclaw_PRE`  
> 结果：已完成一次真实飞书消息测试，机器人成功接收消息、调用 DeepSeek，并在飞书中返回回复。

## 飞书远程审批卡片（待人工后台配置）

危险操作（`edit`、`write`、`powershell`）会发送一张审批卡片。原消息发送人可以选择“允许一次”“允许本次任务”或“拒绝”；群聊里的其他人点击不会放行。

后台只需做一次：进入当前应用的“事件与回调”，保持长连接与 `im.message.receive_v1` 不变，新增 `card.action.trigger`，保存后创建并发布新版本。无需公网地址、Webhook URL 或内网穿透。

验收步骤：

1. 在项目根目录运行 `npm run desktop:dev`，等待 `ws client ready`。
2. 让机器人在不重要的测试目录执行一个会触发写入或 PowerShell 的任务。
3. 确认出现“MiniClaw 操作审批”卡片；原发送账号验证一次允许和一次拒绝。
4. 在群聊由另一账号点击同一张卡片，确认操作仍待审批。
5. 不点击等待 60 秒，确认卡片变为“已过期”且操作未执行。

安全限制：电脑、MiniClaw 进程、飞书长连接与模型配置必须在线；卡片发送失败、超时、任务结束或应用退出都会默认拒绝。卡片仅展示脱敏摘要与工作目录，不会显示 API Key、Secret、Token、Password 或完整敏感输入。

## 一、最终采用的连接方式

本项目使用飞书官方 SDK 的**长连接（WebSocket）**接收事件。

- 不需要公网 IP。
- 不需要配置域名。
- 不需要使用内网穿透或 `cloudflared`。
- 本地程序必须保持运行，并且电脑需要能够访问互联网。

工作流程如下：

```text
飞书用户发送消息
  → 飞书开放平台通过长连接推送事件
  → miniclaw_PRE 接收并解析消息
  → 创建或复用 Agent 会话
  → DeepSeek 生成回答
  → 调用飞书发送消息 API
  → 用户在飞书中看到机器人回复
```

## 二、飞书开发者后台配置

### 1. 创建并进入企业自建应用

在[飞书开放平台](https://open.feishu.cn/app)进入对应的企业自建应用。操作时需要登录该应用所属组织内的飞书账号。

注意：本地 `.env` 中的 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET` 必须属于后台正在配置的**同一个应用**。

### 2. 启用机器人能力

在左侧进入“添加应用能力”或“机器人”，确认机器人能力已经启用，并设置机器人名称等基础信息。

### 3. 配置应用权限

在“权限管理”中开通接收消息事件和以机器人身份发送消息所需的权限。若后台提示权限未生效，需要创建并发布新版本。

### 4. 配置事件订阅

进入“事件与回调”，在**事件配置**中完成以下设置：

1. 订阅方式选择“使用长连接接收事件”。
2. 添加事件 `im.message.receive_v1`（接收消息）。
3. 保存配置。

这里配置的是“事件订阅”，不是 HTTP 回调地址。长连接方式不需要填写公网 URL。

### 5. 发布应用版本

进入“版本管理与发布”：

1. 创建新版本。
2. 确认机器人能力、权限和事件订阅都包含在版本中。
3. 提交发布。
4. 等待审核通过或组织管理员批准。

仅在后台保存配置但没有发布新版本时，正式飞书客户端可能仍然收不到事件。

## 三、本地环境变量

项目根目录使用 `.env` 保存配置，格式如下：

```dotenv
DEEPSEEK_API_KEY=你的新DeepSeek密钥
FEISHU_APP_ID=你的飞书AppID
FEISHU_APP_SECRET=你的飞书AppSecret
FEISHU_CONNECTION_MODE=websocket
```

注意事项：

- 等号两边不要加 `**`、空格或 Markdown 格式。
- 不要给值加中文引号。
- `.env` 不应提交到 Git，也不要截图或发送给其他人。
- `FEISHU_CONNECTION_MODE` 不填写时，当前代码也默认使用 `websocket`。

## 四、代码侧完成的改造

本次在项目中完成了以下功能：

- 安装并接入飞书官方 Node.js SDK。
- 服务启动时自动建立飞书 WebSocket 长连接。
- 服务关闭时自动断开连接。
- 将飞书消息映射到项目统一的 `InboundMessage`。
- 同一飞书会话复用同一个 Agent Run，保留上下文。
- Agent 生成回答后，通过飞书消息 API 发回原会话。
- 同时支持飞书的纯文本消息 `text` 和富文本消息 `post`。
- 增加自动化测试，覆盖长连接启动、事件分发、停止和富文本解析。

主要改动文件：

- `src/im/feishu.ts`
- `src/im/index.ts`
- `src/index.ts`
- `tests/im.test.ts`
- `.env.example`
- `package.json`
- `package-lock.json`

## 五、启动和停止方法

### 普通开发启动

在项目根目录打开 PowerShell：

```powershell
npm run dev
```

### 按当前方式直接启动

```powershell
node --env-file=.env --import tsx src/index.ts
```

看到以下关键信息说明服务和飞书长连接已启动：

```text
MiniAgent server is running at http://localhost:3000
IM transports: started
ws client ready
```

停止服务时，在对应终端按 `Ctrl+C`。关闭终端、退出 Node 进程或关闭电脑后，机器人会离线。

## 六、真实测试过程与结果

测试步骤：

1. 启动本地服务并看到 `ws client ready`。
2. 在飞书中打开机器人单聊。
3. 向机器人发送一条普通消息。
4. 程序收到 `im.message.receive_v1` 事件。
5. 程序创建飞书会话与 Agent Run。
6. DeepSeek 生成回答。
7. 程序调用飞书发送消息 API。
8. 飞书客户端成功显示机器人回复。

最终验证结果：

- 飞书真实事件接收成功。
- 会话创建成功。
- 使用模型：`deepseek/deepseek-v4-pro`。
- Agent 正常完成生成。
- 飞书客户端确认收到机器人回复。
- 自动化测试：49 项全部通过。
- TypeScript 类型检查通过。
- 项目构建通过。

## 七、本次遇到的问题和原因

### 问题 1：配置了后台但消息没有进入程序

原因：事件接收方式或事件订阅页面没有配置正确，或者修改后没有发布新版本。

处理：在“事件配置”中选择长连接，订阅 `im.message.receive_v1`，然后发布新版本。

### 问题 2：服务关闭后机器人不回复

原因：长连接由本地 Node.js 程序维持。程序停止后，飞书无法把事件送到机器人业务代码。

处理：保持程序运行，或将程序设置为开机自动启动/部署到长期在线的服务器。

### 问题 3：后台显示事件成功，但程序没有回复

原因：飞书把输入框发送的内容标记为富文本 `post`，旧代码只接收纯文本 `text`，因此消息被忽略。

处理：代码已加入 `post` 富文本解析，并保留对 `text` 的支持。

### 问题 4：App ID 与后台页面不是同一个应用

原因：本地凭证和当前后台打开的应用不一致时，会导致机器人、事件和权限配置对不上。

处理：确认 `.env` 中的 App ID/Secret 与正在配置和发布的应用完全对应。

## 八、日常使用方法

每次使用前：

1. 确保电脑联网。
2. 启动项目服务。
3. 等待日志出现 `ws client ready`。
4. 在飞书中直接给机器人发消息。

可用命令：

- `/help`：查看机器人命令。
- `/new` 或 `/reset`：清空当前飞书会话上下文并新建 Run。
- `/status`：查看当前 Run 状态。
- `/abort`：中止正在执行的任务。

机器人连接的是一个具备项目目录工具权限的编码 Agent。发送到飞书的内容会被当作真实任务执行，因此测试时建议发送“你好，请简单回复一句”等明确、安全的内容，而不是容易被理解为代码修改命令的文字。

## 九、接下来的建议

目前功能已经能真实使用，但服务依赖这台电脑上的进程持续运行。下一步推荐二选一：

1. **先做 Windows 开机自动启动**：适合个人使用，成本最低。
2. **部署到云服务器长期运行**：适合 24 小时在线和更稳定的正式使用。

采用长连接时，即使部署到云服务器，仍然不要求为飞书事件配置公网回调地址；服务器只需要能够主动访问飞书和 DeepSeek。

## 十、安全处理（必须完成）

本次调试过程中，真实的 DeepSeek API Key 和飞书 App Secret 曾被直接粘贴到聊天中。应当把它们视为已经暴露：

1. 到 DeepSeek 控制台删除或重置旧 API Key，并创建新 Key。
2. 到飞书开发者后台重置 App Secret。
3. 把新值写入本地 `.env`。
4. 重启服务。
5. 不要把新密钥写进本文档、Git 提交、截图或聊天消息。

